meta {
  name: update user
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/users/update/:id
  body: json
  auth: bearer
}

params:path {
  id: 4
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "username": "updatedUser",
    "email": "updatedEmail@gmail.com"
  }
}
